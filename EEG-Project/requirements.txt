# Core scientific computing
numpy>=1.25.0
scipy>=1.11.0
pandas>=2.0.0

# EEG processing / I/O
mne>=1.5.0
autoreject>=0.4.3
python-picard>=0.7          # ICA (Picard)
pyedflib>=0.1.36            # optional EDF backend; improves EDF I/O in some environments

# Connectivity / signal features (optional but used in some analyses)
mne-connectivity>=0.7.0     # coherence / connectivity utilities

# Spectral parameterization (aperiodic/peaks) (optional; used if you fit aperiodic exponent/IAF)
specparam>=2.0.0rc0            # formerly FOOOF

# Machine learning / stats
scikit-learn>=1.3.0
statsmodels>=0.14.0         # FDR / multipletests, etc.
joblib>=1.3.0               # model persistence (also used by sklearn)

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars
tqdm>=4.65.0

# Data formats / storage (optional)
h5py>=3.8.0                 # large arrays / HDF5
nibabel>=5.0.0              # only needed if you use neuroimaging file formats

# Notebook support (optional)
jupyterlab>=4.0.0
ipykernel>=6.25.0
ipywidgets>=8.0.0

# Demo / API server (optional; only needed if running prediction_server.py)
flask>=3.0.0
flask-cors>=4.0.0

# Pytest
pytest>=7.4.0
